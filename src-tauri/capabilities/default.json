{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability with only the permissions required by the app",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:allow-listen"
  ]
}
